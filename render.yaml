services:
  - type: web
    name: telegram-mega-bot
    runtime: python
    env: 
      - key: PYTHON_VERSION
        value: 3.10
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python bot.py"
    envVars:
      - fromFile: .env
